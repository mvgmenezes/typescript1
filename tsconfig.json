{
    "compilerOptions": {
        "target": "es6",
        "outDir": "app/js", /* diretorio que os js's serao gerados*/
        "noEmitOnError": true,
        "noImplicitAny": true, /* obriga a definicao da variavel ao inves de assumir any */
        "removeComments": true,/* remove os comentarios para quando for gerado os js, assim em producao nao apresenta o erro */
        "module": "system", /* para nao precisar incluir todos os imports no html o system.js já resolve isso*/
        "strictNullChecks": true /* nao permite atribuir null ou undefined a variaives diferentes do tipo null*/
    },
    "include": [
        "app/ts/**/*"
    ]
}